import React from "react";
import { View, Text } from "react-native";
import { gql, useApolloClient, useMutation, useQuery } from "@apollo/client";

const Pessoas = () => {
    return (
        <View>
            <Text>Pessoas tela</Text>
        </View>
    )
}

export default Pessoas